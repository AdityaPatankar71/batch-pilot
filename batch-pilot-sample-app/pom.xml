<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.batchpilot</groupId>
        <artifactId>batch-pilot-parent</artifactId>
        <version>0.1.0</version>
    </parent>

    <artifactId>batch-pilot-sample-app</artifactId>
    <packaging>jar</packaging>
    <name>batch-pilot-sample-app</name>
    <description>Demo Spring Boot app with three batch jobs, wired to the batch-pilot console.</description>

    <properties>
        <!-- Demo module: never published to Maven Central. maven.deploy.skip
             stops the classic maven-deploy-plugin. For the central-publishing
             bundle this module is excluded at deploy time via
             `-pl '!batch-pilot-sample-app'` (see RELEASING.md): the plugin
             aggregates the whole reactor and offers no reliable per-module
             skip when the module is the reactor's upload aggregator. -->
        <maven.deploy.skip>true</maven.deploy.skip>
    </properties>

    <dependencies>
        <dependency>
            <groupId>io.github.batchpilot</groupId>
            <artifactId>batch-pilot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.batch</groupId>
            <artifactId>spring-batch-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
