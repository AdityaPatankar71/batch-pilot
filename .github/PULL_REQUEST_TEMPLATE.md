<!-- Thanks for contributing to batch-pilot! -->

## What & why

<!-- What does this change and why? Link the issue it closes, e.g. "Closes #12". -->

## How tested

<!-- e.g. `mvn clean install` passes; manually verified in the sample app. -->

## Checklist

- [ ] `mvn clean install` passes (UI build + tests included)
- [ ] UI changes keep the loading / error / empty-state pattern and work in light **and** dark mode
- [ ] No hard-coded colors (use the CSS variables / M3 themes in `styles.scss`)
- [ ] Icon-only buttons have `aria-label`s
